class CellInfo {
  
  int x; //x coordinate in grid
  int y; //y coordinate in grid
  int counter;  //distance in # of cells from destination cel
  
  CellInfo(int x, int y, int counter) {
    this.x=x;
    this.y=y;
    this.counter=counter;
  }
  
}